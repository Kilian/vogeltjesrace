---
import prijzen from "../data/prijzen.js";

let uitslag = await Astro.glob("../pages/uitslagen/*.md");
const prijswinnaars = uitslag[0].frontmatter.uitslag.split("\n");

// Component Imports
import BaseHead from "../components/BaseHead.astro";
import Logobar from "../components/Logobar.astro";
import Charity from "../components/Charity.astro";
import Sponsors from "../components/Sponsors.astro";
import Footer from "../components/Footer.astro";

import { SITE } from "../config";
import SmallHeader from "../components/SmallHeader.astro";

let title = `Uitslag | ${SITE.title}`;
let description = "De uitslagen van de Vogeltjesrace 2023 Zoetermeer.";
let permalink = "https://vogeltjesrace.nl/uitslag/";
---

<html lang="nl">
  <head>
    <BaseHead {title} {description} {permalink} />
    <style>
      @media (prefers-reduced-motion: no-preference) {
        html {
          scroll-behavior: smooth;
        }
      }

      .formcontainer {
        background: #fff;
        color: #2d4475;
        padding: 4rem 2rem;
      }
      .inner {
        position: relative;
        margin: 0 auto;
        max-width: 45rem;
      }

      h1 {
        font-size: 3rem;
        max-width: 100%;
        font-family: Boogaloo;
        color: #f08535;
        line-height: 1;
        font-weight: bold;
        text-align: center;
        word-wrap: break-word;
      }

      p {
        text-align: center;
      }

      h2 {
        font-size: 1.25rem;
        max-width: 100%;
        font-family: inherit;
        color: #f08535;
        line-height: 1;
        font-weight: normal;
        text-align: center;
        margin: 0;
        padding: 0;
      }

      h2 span {
        font-family: Boogaloo;
        color: red;
        font-weight: bold;
        display: block;
        font-size: 2rem;
        margin-top: 0.5rem;
      }

      ul {
        padding: 0;
        list-style: none;
        max-width: 50ch;
        margin: 0 auto;
      }
      ul li {
        margin-bottom: 2rem;
        text-align: center;
      }

      ul small {
        display: block;
      }

      ul .hello {
        padding-top: 2rem;
      }

      ul .hello:not(:first-child)::before {
        content: " ";
        display: block;
        width: 100%;
        height: 1px;
        background: rgb(166, 178, 42);
        margin-bottom: 4rem;
      }
    </style>
  </head>

  <body>
    <SmallHeader />
    <div class="formcontainer">
      <div class="inner">
        <h1>Uitslagen</h1>
        <p>
          <b> De uitslagen van de vogeltjesrace zijn binnen, zie hieronder of je gewonnen hebt!</b> Prijzen kan je ophalen
          bij de infostand of na afloop van de Vogeltjesrace in de Wereldwinkel in Dorpsstraat (tot en met 31 oktober)
        </p>

        <ul>
          {
            prijzen.map((prijs, index) => (
              <li class={index % 10 === 0 ? "hello" : ""}>
                <h2>
                  {prijs.id}e plaats <span>{prijswinnaars[index] || ""}</span>
                </h2>
                <b>{prijs.prijs}</b>
                <small>aangeboden door {prijs.aanbieder}</small>
              </li>
            ))
          }
        </ul>
      </div>
    </div>
    <Logobar />
    <Footer />
  </body>
</html>
